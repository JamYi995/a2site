# 安全政策

## 报告安全问题

请不要通过公开 Issue 披露尚未修复的漏洞。

在正式安全邮箱建立前，请通过果酱盒子官方联系方式提交，并在标题中注明 `A2Site Security`。不要发送真实生产密钥、用户验证码或不必要的个人数据。

## 支持范围

当前维护分支为 `main`。项目尚处于早期版本，正式生产使用前应自行完成威胁建模、渗透测试、备份恢复和依赖安全检查。

## 默认安全边界

- A2Site 不提供 Agent 推理能力。
- 高风险操作必须由独立人类确认。
- 生产端点必须使用 HTTPS。
- 示例配置不得用于正式生产。
