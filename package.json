{
  "name": "a2site",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm --filter @a2site/protocol build && pnpm --filter @a2site/database build && pnpm --filter @a2site/identity build && pnpm --filter @a2site/fastify build && pnpm --filter @a2site/gateway build",
    "dev": "pnpm --filter @a2site/gateway dev",
    "start": "pnpm --filter @a2site/gateway start",
    "test": "pnpm build && pnpm -r test",
    "typecheck": "pnpm build && pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
