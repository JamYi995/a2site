{
  "name": "@a2site/gateway",
  "version": "0.2.0",
  "private": true,
  "description": "A2Site 可独立部署的网站 Agent 接入网关",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@a2site/database": "workspace:*",
    "@a2site/fastify": "workspace:*",
    "@a2site/identity": "workspace:*",
    "@a2site/protocol": "workspace:*",
    "dotenv": "^17.2.2",
    "fastify": "^5.6.1",
    "nodemailer": "^9.0.1"
  },
  "devDependencies": {
    "@types/nodemailer": "^7.0.1"
  }
}
